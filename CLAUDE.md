## Agent skills

### Issue tracker

Issues live in GitHub Issues for `hypnos1450/Conduit`, using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context repo — `CONTEXT.md` + `docs/adr/` at the repo root (created lazily as needed). See `docs/agents/domain.md`.
